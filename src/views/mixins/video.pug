mixin video(video)
    div 
        h4
            a(href=`/videos/${video.id}`)=video.title
        ul  
            li  #{video.rating}/10
            li  #{video.views} people watch
            li  #{video.comments}
            li  #{video.createdAt}
            li  id: #{video.id}